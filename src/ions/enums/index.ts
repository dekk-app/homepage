export enum Template {
	home = "home",
	dashboard = "dashboard",
	create = "create",
}

export enum Route {
	home = "home",
	dashboard = "dashboard",
	create = "create",
}

export enum Renderer {
	svg = "svg",
	html = "html",
	canvas = "canvas",
}

export enum ShapeType {
	triangle = "triangle",
	rectangle = "rectangle",
	circle = "circle",
}

export enum SnapMethod {
	floor = "floor",
	ceil = "ceil",
	round = "round",
}
