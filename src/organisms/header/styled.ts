import styled from "@emotion/styled";

export const StyledHeader = styled.header`
	display: flex;
	position: relative;
	z-index: 2;
	grid-area: Header;
	padding: 1rem;
	background: blue;
	color: black;
`;
